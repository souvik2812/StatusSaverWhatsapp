package com.example.statussaver.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette — Deep Violet / Electric Blue ──────────────────────────
val NeonViolet       = Color(0xFF7C4DFF)  // Primary action colour
val NeonVioletLight  = Color(0xFFB47CFF)  // On-dark primary container
val NeonVioletDark   = Color(0xFF5A31D4)  // Pressed / shadow state

val ElectricBlue     = Color(0xFF2979FF)  // Secondary / link colour
val ElectricBlueLight= Color(0xFF82B1FF)  // Secondary container tint

val DeepTeal         = Color(0xFF00E5CC)  // Tertiary / accent

// ── Backgrounds ─────────────────────────────────────────────────────────────
val DarkBg           = Color(0xFF0D0D1A)  // Overall dark background
val DarkSurface      = Color(0xFF13132B)  // Card / surface background
val DarkSurfaceVar   = Color(0xFF1C1C3A)  // Slightly lighter surface variant

// ── On-Colours ───────────────────────────────────────────────────────────────
val OnDark           = Color(0xFFE8E8FF)  // Main text on dark backgrounds
val OnDarkVariant    = Color(0xFFAAABC8)  // Secondary / caption text

// ── Status Colours ───────────────────────────────────────────────────────────
val SuccessGreen     = Color(0xFF00C853)
val ErrorRed         = Color(0xFFFF5252)
val DownloadingAmber = Color(0xFFFFD740)
